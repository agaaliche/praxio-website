# Database Configuration
# For local development (TCP connection)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=master
DB_PORT=3306

# For Cloud Run deployment (Unix socket - set by deploy script)
# DB_SOCKET_PATH=/cloudsql/inrmanager-97748:us-central1:inr-database

# Stripe Configuration
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_PUBLISHABLE_KEY=pk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Firebase (for email sending via Firebase Extension)
USE_FIREBASE_EMAIL=true
FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}

# Site URL (update for production)
SITE_URL=http://localhost:3000

# Retroact SSO URL (update for production)
RETROACT_URL=http://localhost:8081

# Public API URL (for client-side API calls during transition)
NUXT_PUBLIC_API_URL=https://inr-backend-pq7nv4e3fq-uk.a.run.app
